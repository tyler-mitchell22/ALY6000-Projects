library(testthat)

123 * 453
5^2 * 40
TRUE & FALSE
TRUE | FALSE
75 %% 10
75 / 10

first_vector <- c(17, 12, -33, 5)

counting_by_fives <- seq(5, 35, 5)

second_vector <- 20:1

counting_vector <- 5:15

grades <- c(96, 100, 85, 92, 81, 72)

bonus_points_added <- grades + 3

one_to_one_hundred <- 1:100

# This is adding 20 to every value in the variable: second_vector
second_vector + 20

# This is multiplying every value in second_vector by 20
second_vector * 20

# This is stating whether each value in second_vector is greater than or equal to 20 
second_vector >= 20

# This is stating whether each value in second_vector does not equal 20
second_vector != 20

total <- sum(one_to_one_hundred)

average_value <- mean(one_to_one_hundred)

median_value <- median(one_to_one_hundred)

max_value <- max(one_to_one_hundred)

min_value <- min(one_to_one_hundred)

first_value <- second_vector[c(1)]

first_three_values <- second_vector[c(1:3)]

vector_from_brackets <- second_vector[c(1, 5, 10, 11)]

vector_from_boolean_brackets <- first_vector[c(FALSE, TRUE, FALSE, TRUE)]

# This is displaying whether each value in second_vector is greater than or equal to 10
second_vector >= 10

# This is pull all values from one_to_one_hundred that are greater than or equal to 20
one_to_one_hundred[one_to_one_hundred >= 20]

lowest_grades_removed <- grades[c(1, 2, 4)]

middle_grades_removed <- grades[c(-3, -4)]

fifth_vector <- second_vector[c(-5, -10)]

set.seed(5)
random_vector <- runif(n = 10, min = 0, max = 1000)

sum_vector <- sum(random_vector)

cumsum_vector <- cumsum(random_vector)

mean_vector <- mean(random_vector)

sd_vector <- sd(random_vector)

round_vector <- round(random_vector)

sort_vector <- sort(random_vector)

first_datafram <- read.csv("ds_salaries.csv")

summary(first_datafram)

